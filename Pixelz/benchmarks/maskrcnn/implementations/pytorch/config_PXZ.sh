## DL params
: "${ENABLE_DALI:=False}"
: "${USE_CUDA_GRAPH:=False}"
: "${CACHE_EVAL_IMAGES:=False}"
: "${EVAL_SEGM_NUMPROCS:=10}"
: "${EVAL_MASK_VIRTUAL_PASTE:=False}"
: "${INCLUDE_RPN_HEAD:=True}"
: "${PRECOMPUTE_RPN_CONSTANT_TENSORS:=True}"
: "${DATALOADER_NUM_WORKERS:=1}"
: "${HYBRID_LOADER:=True}"
: "${SOLVER_MAX_ITER:=12000}"

export EXTRA_PARAMS=""
export EXTRA_CONFIG='SOLVER.BASE_LR 0.2 SOLVER.WARMUP_FACTOR 0.000192 SOLVER.WARMUP_ITERS 625 SOLVER.WARMUP_METHOD linear SOLVER.STEPS (7200,9600) SOLVER.IMS_PER_BATCH 160 TEST.IMS_PER_BATCH 160 MODEL.RPN.FPN_POST_NMS_TOP_N_TRAIN 15000 MODEL.RPN.FPN_POST_NMS_TOP_N_PER_IMAGE False NHWC True'
export EXTRA_CONFIG="${EXTRA_CONFIG} SOLVER.MAX_ITER ${SOLVER_MAX_ITER} DATALOADER.DALI $ENABLE_DALI DATALOADER.DALI_ON_GPU $ENABLE_DALI DATALOADER.CACHE_EVAL_IMAGES $CACHE_EVAL_IMAGES EVAL_SEGM_NUMPROCS $EVAL_SEGM_NUMPROCS USE_CUDA_GRAPH $USE_CUDA_GRAPH EVAL_MASK_VIRTUAL_PASTE $EVAL_MASK_VIRTUAL_PASTE MODEL.BACKBONE.INCLUDE_RPN_HEAD $INCLUDE_RPN_HEAD DATALOADER.NUM_WORKERS $DATALOADER_NUM_WORKERS PRECOMPUTE_RPN_CONSTANT_TENSORS $PRECOMPUTE_RPN_CONSTANT_TENSORS DATALOADER.HYBRID $HYBRID_LOADER"


## System run parms
export NEXP=5
export PXZNNODES=1
export PXZSYSTEM=PXZ

WALLTIME_MINUTES=200
export WALLTIME=$((${NEXP} * ${WALLTIME_MINUTES}))

## System config params
export PXZNGPU=8
export PXZSOCKETCORES=32
export PXZNSOCKET=1
export PXZHT=1         # HT is on is 2, HT off is 1
